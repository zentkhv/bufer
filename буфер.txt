if crontab -l | grep -q "$(cat "$(dirname "$0")/cron_metrics")"; then
  echo "Задание уже существует в crontab."
else
  (crontab -l ; echo "$(cat $(dirname "$0")/cron_metrics)") | crontab -
  echo "Задание успешно добавлено в crontab."
fi