#!/bin/bash

if [ $# -eq 2 ]; then
    if [ "$1" = "True" ]; then
        echo "Первый параметр: $1"
    fi

    if [ "$2" = "True" ]; then
        echo "Второй параметр: $2"
    fi
else
    echo "Ошибка: нужно передать два параметра"
    exit 1
fi
